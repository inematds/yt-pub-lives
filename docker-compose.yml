services:
  dashboard:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./config:/config        # .env, credentials.enc, .encryption_key
      - ./lives:/data/lives     # videos processados
    env_file:
      - ./config/.env
    restart: unless-stopped
